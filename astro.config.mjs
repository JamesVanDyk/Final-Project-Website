// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	base: "/Final-Project-Website", // replace 'repo-name' with your GitHub repository name
});
